<% if @watchlist.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@watchlist.errors.count, "error") %> prohibited this watchlist from being saved:</h2>

    <ul>
      <% @watchlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for(@watchlist) do |f| %>
    <%= f.label :category_type, "Category:" %> 
    <%= f.text_field :category_type, placeholder: "Name your watchlist" %><br>
    <%= f.label :user_id, "User:" %> 
    <%= f.number_field :user_id, placeholder: "Enter user's id" %><br>
    <%= f.submit "Submit Watchlist" %>
<% end %>
