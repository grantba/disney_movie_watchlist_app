<ul>
    <li><%= pluralize(review.rating, "Star") %> - <%= review.description %>
    <% if review.user_id == current_user.id %>
    &emsp;&emsp;<%= link_to "Edit", edit_user_review_path(current_user.id, review.id) %>  |  <%= link_to "Delete", review_path(review.id), method: 'delete', data: {confirm: "Are you sure you want to delete this review for #{review.movie.Title}?" } %>
    <% end %>
    </li>
</ul>
